<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kosta.mapper.module.ScheduleMapper">
<select id="listProject" resultType="ScheduleProject">
	select * from Project
</select>
<select id="listCategory" resultType="ScheduleCategory" parameterType="int">
	select * from Category where project_id = #{projectId}
</select>
<select id="listCalender" resultType="ScheduleCalender" parameterType="int">
	select cat.category_id, cat.category_name, cal.calender_id, cal.title, cal.start_dt, cal.end_dt,
	 cal.background_color, cal.completion_per, cal.y_pos from Calender cal
	RIGHT join (select * from Category where project_id=#{projectId}) cat
	ON cal.category_id = cat.category_id
	order by cat.category_id
</select>
<insert id="insertCelender" parameterType="ScheduleCalender">
	insert into Calender values(calender_id_seq.nextval, #{title}, TO_DATE(#{startDt}, 'yy-mm-dd') , TO_DATE(#{endDt}, 'yy-mm-dd') , #{backgroundColor}, #{completionPer}, #{yPos}, #{categoryId})
</insert>

<select id="listProjectCalender" resultType="ScheduleCalenderList" parameterType="int">
	select *
	from (select cat.x_pos, cat.category_id, cat.category_name, cat.project_id,  cal.calender_id, cal.title, cal.start_dt, cal.end_dt,
	cal.background_color, cal.completion_per, cal.y_pos
	from calender cal
	right outer join category cat
	on cal.category_id = cat.category_id) c
	where c.project_id = #{projectId}
	order by c.category_id asc, c.y_pos desc       
</select>


<update id = "editCalender" parameterType="ScheduleCalender">
	 update calender
     <trim prefix="SET" suffixOverrides=",">
       <if test="title != null">
        title = #{title},
       </if>
       <if test="startDt != null">
        start_dt = TO_DATE(#{startDt}, 'yy-mm-dd'),
        </if>
       <if test="endDt != null">
        end_dt= TO_DATE(#{endDt}, 'yy-mm-dd') ,
        </if>
       <if test="backgroundColor != null">
        background_color= #{backgroundColor},
        </if>
       <if test="completionPer != null">
        completion_per= #{completionPer},
        </if>
      </trim>
       where calender_id = #{calenderId}
</update>

<delete id="delCalender" parameterType="int">
	delete from calender where calender_id = #{calenderId}
</delete>
<update id="editCalenderMove" parameterType="ScheduleCalenderMove" >
	update calender set category_id = #{categoryId} , y_pos = #{yPos} where calender_id = #{calenderId}
</update>
<insert id="insertCategory" parameterType="ScheduleCategory">
	insert into Category values(category_id_seq.nextval , #{categoryName}, #{projectId}, #{xPos})
</insert>
<update id="editCategoryName" parameterType="ScheduleCategory">
	update Category set category_name = #{categoryName} where category_id = #{categoryId}
</update>
<insert id="insertProject" parameterType="ScheduleProject">
	insert into project values(project_id_seq.nextval , #{title}, #{projectLeader}, #{projectStartDt}, #{projectEndDt}, #{projectRegDt}, #{projectCompletion}, #{publicProject})
</insert>
<delete id="deleteProject" parameterType="ScheduleProject">
	delete from project where project_id = #{projectId}
</delete>
</mapper>